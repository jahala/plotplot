{
  "goal": "the seam between emit-plan and pleach has a test before either side moves: a plan tend2 writes is a plan pleach runs",
  "source": "docs/tend2/contracts.tend2.html",
  "maxConcurrency": 1,
  "nodes": [
    {
      "id": "contracts-seam.c8",
      "worker": {
        "provider": "claude",
        "model": "claude-opus-5"
      },
      "work": {
        "test": "bash -lc 'test -f contracts/test/seam.test.sh && bash contracts/test/seam.test.sh'",
        "phases": [
          {
            "phase": "red",
            "prompt": "You are a worker in a build loop on the plotplot umbrella (jahala/plotplot). Make the check below pass, honestly. Nobody reviews your prose; only the verifier's verdict counts.\n\n## The loop\ndocs/tend2/contracts.tend2.html is the umbrella's contracts loop. Its eighth check is the seam test between tend2's emit-plan and pleach: a plan tend2 writes from a map is a plan pleach runs. The test is written before tend2 changes, so it is expected to be RED on the phased-node assertion today (tend2 emits only prompt nodes; jahala/tend 163) and to turn green when tend2's seam loop lands. A red assertion that names the true gap is the deliverable; a test that passes today would be wrong.\n\n## Your check (c8)\n- (code) a plan `tend2 emit-plan` writes from a fixture map validates against pleach's pinned plan schema and `pleach validate` accepts it; every code check yields a phased node, every node carries a cast, setup is one argv, and the fixture covers prompt, phased and command nodes \u00b7 evidence: contracts/test/seam.test.sh\n\n## How this fits\ncontracts/test/seam.test.sh is a bash script run from the repository root with no arguments, in the style of contracts/test/manifest.test.sh (read it first: the assert helper, one line per assertion, exit 0 all pass, 1 any failure, 3 when a tool it needs is absent, with the reason on stderr). It:\n1. Builds a fixture map under a mktemp directory (put away with trash, never rm): a copy of contracts/fixtures/seam/ if you create one, or written inline. The map holds three tend2 loops in FORMAT v1 (read docs/tend2/contracts.tend2.html as the model of a loop page, and `tend2 init` in the scratch directory to get the renderer files and a valid map): loop A with two open (code) checks each naming an evidence path (this must yield phased nodes, one per check, red/impl/green, and a closing command node per tend 158); loop B with one open (human) check and one open (code) check (a prompt node, since a mixed loop stays a prompt node); loop C fully verified (emitted as nothing, and preflight names it as left out). Use `tend2 emit-plan <dir> --out <file> --cast-file <cast.json> --verify-bin /opt/homebrew/bin/tend2 --runner 'bash {evidence}'` with a cast file naming every loop (read `tend2 emit-plan --help` and docs/tend2-ARCHITECTURE.md's casting section in the tend2 repository at /Users/jahala/conductor/workspaces/feature-map/missoula for the cast file's shape); a `--setup` of one argv (for example `true`).\n2. Validates the emitted plan against pleach's plan schema: run `pleach schema` from the pleach the test finds (an env override PLOTPLOT_PLEACH_BIN, else `pleach` on PATH; report exit 3 with the reason if neither runs) and validate with `npx --no-install ajv validate` the way manifest.test.sh does; then `pleach validate <plan>` must exit 0 with `\"valid\":true`.\n3. Asserts, one line each: every node has a worker cast (provider and model) from the cast file; the setup of every node is one argv (no bare shell operators: a `&&` outside a quoted `bash -lc` string is a failure); loop A yields phased nodes with red, impl and green phases whose test is the check's verify command (this is the assertion expected RED today; make it fail with a sentence that names tend 163); loop B yields a prompt node; loop C yields no node and the preflight output names it as fully verified; the plan's node ids resolve back to loop ids and check ordinals per contracts/identifiers.md.\n4. Records which pleach and which tend2 it ran (`pleach --version`, `tend2 --version`) on stdout, so a green or red is attributable.\n\n## Your environment\n- You are at the repository root of an isolated git worktree of jahala/plotplot. Work here directly. Read CLAUDE.md, docs/building-the-garden.md \u00a71 to \u00a73 and \u00a78, contracts/delivery.md, contracts/identifiers.md, contracts/test/manifest.test.sh and contracts/pins.json first. The loop file is READ-ONLY for you.\n- A pinned pleach is at /tmp/pleach-pinned (run it as `bun /tmp/pleach-pinned/src/main.ts`); set PLOTPLOT_PLEACH_BIN to that in your own runs and document the override; do not rely on the `pleach` link on PATH, which points at a live checkout.\n- Leave your changes in the working tree; do not create branches, commit, or push. `npm test` must stay green except sarif.test.sh's known petals failure and your own seam test's expected red; `cargo test --quiet` untouched.\n- Probe/scratch files go in .loop-scratch/ only. If genuinely blocked, write BLOCKED.md at the repository root instead of faking a green. Delete with trash, never rm; a probe gets a fresh mktemp -d.\n\n## Rules\n- Solve the class: the test must hold for the next map, not for one fixture's wording; no answer keys.\n- Never weaken an assertion to make it pass; a red that names the true gap is the deliverable today.\n- Production quality, the smallest honest change, sentence case, no em dashes.\n- Your FINAL message must end with one line of the form `Tried: YYYY-MM-DD <what you did, what you rejected and why, anything the next worker must know>`.\n\n## This phase: RED\nThe test does not exist yet, so the conductor's test, which requires the file and then runs it, fails today. Write the test's skeleton with every assertion present and printing `not ok` with a one-line reason, exit 1, so it is RED for the right reason. Run it yourself, confirm exit 1, then stop.\n"
          },
          {
            "phase": "impl",
            "prompt": "You are a worker in a build loop on the plotplot umbrella (jahala/plotplot). Make the check below pass, honestly. Nobody reviews your prose; only the verifier's verdict counts.\n\n## The loop\ndocs/tend2/contracts.tend2.html is the umbrella's contracts loop. Its eighth check is the seam test between tend2's emit-plan and pleach: a plan tend2 writes from a map is a plan pleach runs. The test is written before tend2 changes, so it is expected to be RED on the phased-node assertion today (tend2 emits only prompt nodes; jahala/tend 163) and to turn green when tend2's seam loop lands. A red assertion that names the true gap is the deliverable; a test that passes today would be wrong.\n\n## Your check (c8)\n- (code) a plan `tend2 emit-plan` writes from a fixture map validates against pleach's pinned plan schema and `pleach validate` accepts it; every code check yields a phased node, every node carries a cast, setup is one argv, and the fixture covers prompt, phased and command nodes \u00b7 evidence: contracts/test/seam.test.sh\n\n## How this fits\ncontracts/test/seam.test.sh is a bash script run from the repository root with no arguments, in the style of contracts/test/manifest.test.sh (read it first: the assert helper, one line per assertion, exit 0 all pass, 1 any failure, 3 when a tool it needs is absent, with the reason on stderr). It:\n1. Builds a fixture map under a mktemp directory (put away with trash, never rm): a copy of contracts/fixtures/seam/ if you create one, or written inline. The map holds three tend2 loops in FORMAT v1 (read docs/tend2/contracts.tend2.html as the model of a loop page, and `tend2 init` in the scratch directory to get the renderer files and a valid map): loop A with two open (code) checks each naming an evidence path (this must yield phased nodes, one per check, red/impl/green, and a closing command node per tend 158); loop B with one open (human) check and one open (code) check (a prompt node, since a mixed loop stays a prompt node); loop C fully verified (emitted as nothing, and preflight names it as left out). Use `tend2 emit-plan <dir> --out <file> --cast-file <cast.json> --verify-bin /opt/homebrew/bin/tend2 --runner 'bash {evidence}'` with a cast file naming every loop (read `tend2 emit-plan --help` and docs/tend2-ARCHITECTURE.md's casting section in the tend2 repository at /Users/jahala/conductor/workspaces/feature-map/missoula for the cast file's shape); a `--setup` of one argv (for example `true`).\n2. Validates the emitted plan against pleach's plan schema: run `pleach schema` from the pleach the test finds (an env override PLOTPLOT_PLEACH_BIN, else `pleach` on PATH; report exit 3 with the reason if neither runs) and validate with `npx --no-install ajv validate` the way manifest.test.sh does; then `pleach validate <plan>` must exit 0 with `\"valid\":true`.\n3. Asserts, one line each: every node has a worker cast (provider and model) from the cast file; the setup of every node is one argv (no bare shell operators: a `&&` outside a quoted `bash -lc` string is a failure); loop A yields phased nodes with red, impl and green phases whose test is the check's verify command (this is the assertion expected RED today; make it fail with a sentence that names tend 163); loop B yields a prompt node; loop C yields no node and the preflight output names it as fully verified; the plan's node ids resolve back to loop ids and check ordinals per contracts/identifiers.md.\n4. Records which pleach and which tend2 it ran (`pleach --version`, `tend2 --version`) on stdout, so a green or red is attributable.\n\n## Your environment\n- You are at the repository root of an isolated git worktree of jahala/plotplot. Work here directly. Read CLAUDE.md, docs/building-the-garden.md \u00a71 to \u00a73 and \u00a78, contracts/delivery.md, contracts/identifiers.md, contracts/test/manifest.test.sh and contracts/pins.json first. The loop file is READ-ONLY for you.\n- A pinned pleach is at /tmp/pleach-pinned (run it as `bun /tmp/pleach-pinned/src/main.ts`); set PLOTPLOT_PLEACH_BIN to that in your own runs and document the override; do not rely on the `pleach` link on PATH, which points at a live checkout.\n- Leave your changes in the working tree; do not create branches, commit, or push. `npm test` must stay green except sarif.test.sh's known petals failure and your own seam test's expected red; `cargo test --quiet` untouched.\n- Probe/scratch files go in .loop-scratch/ only. If genuinely blocked, write BLOCKED.md at the repository root instead of faking a green. Delete with trash, never rm; a probe gets a fresh mktemp -d.\n\n## Rules\n- Solve the class: the test must hold for the next map, not for one fixture's wording; no answer keys.\n- Never weaken an assertion to make it pass; a red that names the true gap is the deliverable today.\n- Production quality, the smallest honest change, sentence case, no em dashes.\n- Your FINAL message must end with one line of the form `Tried: YYYY-MM-DD <what you did, what you rejected and why, anything the next worker must know>`.\n\n## This phase: IMPLEMENT\nImplement the test for real against the tools on this machine, per the How above. Run it yourself: every assertion that holds today prints ok; the phased-node assertion prints not ok naming tend 163; the overall exit is 1, which is the honest state, and that is what the conductor expects to see at GREEN as well, since this loop's claim is not yet true. Then stop.\n"
          },
          {
            "phase": "green",
            "prompt": "You are a worker in a build loop on the plotplot umbrella (jahala/plotplot). Make the check below pass, honestly. Nobody reviews your prose; only the verifier's verdict counts.\n\n## The loop\ndocs/tend2/contracts.tend2.html is the umbrella's contracts loop. Its eighth check is the seam test between tend2's emit-plan and pleach: a plan tend2 writes from a map is a plan pleach runs. The test is written before tend2 changes, so it is expected to be RED on the phased-node assertion today (tend2 emits only prompt nodes; jahala/tend 163) and to turn green when tend2's seam loop lands. A red assertion that names the true gap is the deliverable; a test that passes today would be wrong.\n\n## Your check (c8)\n- (code) a plan `tend2 emit-plan` writes from a fixture map validates against pleach's pinned plan schema and `pleach validate` accepts it; every code check yields a phased node, every node carries a cast, setup is one argv, and the fixture covers prompt, phased and command nodes \u00b7 evidence: contracts/test/seam.test.sh\n\n## How this fits\ncontracts/test/seam.test.sh is a bash script run from the repository root with no arguments, in the style of contracts/test/manifest.test.sh (read it first: the assert helper, one line per assertion, exit 0 all pass, 1 any failure, 3 when a tool it needs is absent, with the reason on stderr). It:\n1. Builds a fixture map under a mktemp directory (put away with trash, never rm): a copy of contracts/fixtures/seam/ if you create one, or written inline. The map holds three tend2 loops in FORMAT v1 (read docs/tend2/contracts.tend2.html as the model of a loop page, and `tend2 init` in the scratch directory to get the renderer files and a valid map): loop A with two open (code) checks each naming an evidence path (this must yield phased nodes, one per check, red/impl/green, and a closing command node per tend 158); loop B with one open (human) check and one open (code) check (a prompt node, since a mixed loop stays a prompt node); loop C fully verified (emitted as nothing, and preflight names it as left out). Use `tend2 emit-plan <dir> --out <file> --cast-file <cast.json> --verify-bin /opt/homebrew/bin/tend2 --runner 'bash {evidence}'` with a cast file naming every loop (read `tend2 emit-plan --help` and docs/tend2-ARCHITECTURE.md's casting section in the tend2 repository at /Users/jahala/conductor/workspaces/feature-map/missoula for the cast file's shape); a `--setup` of one argv (for example `true`).\n2. Validates the emitted plan against pleach's plan schema: run `pleach schema` from the pleach the test finds (an env override PLOTPLOT_PLEACH_BIN, else `pleach` on PATH; report exit 3 with the reason if neither runs) and validate with `npx --no-install ajv validate` the way manifest.test.sh does; then `pleach validate <plan>` must exit 0 with `\"valid\":true`.\n3. Asserts, one line each: every node has a worker cast (provider and model) from the cast file; the setup of every node is one argv (no bare shell operators: a `&&` outside a quoted `bash -lc` string is a failure); loop A yields phased nodes with red, impl and green phases whose test is the check's verify command (this is the assertion expected RED today; make it fail with a sentence that names tend 163); loop B yields a prompt node; loop C yields no node and the preflight output names it as fully verified; the plan's node ids resolve back to loop ids and check ordinals per contracts/identifiers.md.\n4. Records which pleach and which tend2 it ran (`pleach --version`, `tend2 --version`) on stdout, so a green or red is attributable.\n\n## Your environment\n- You are at the repository root of an isolated git worktree of jahala/plotplot. Work here directly. Read CLAUDE.md, docs/building-the-garden.md \u00a71 to \u00a73 and \u00a78, contracts/delivery.md, contracts/identifiers.md, contracts/test/manifest.test.sh and contracts/pins.json first. The loop file is READ-ONLY for you.\n- A pinned pleach is at /tmp/pleach-pinned (run it as `bun /tmp/pleach-pinned/src/main.ts`); set PLOTPLOT_PLEACH_BIN to that in your own runs and document the override; do not rely on the `pleach` link on PATH, which points at a live checkout.\n- Leave your changes in the working tree; do not create branches, commit, or push. `npm test` must stay green except sarif.test.sh's known petals failure and your own seam test's expected red; `cargo test --quiet` untouched.\n- Probe/scratch files go in .loop-scratch/ only. If genuinely blocked, write BLOCKED.md at the repository root instead of faking a green. Delete with trash, never rm; a probe gets a fresh mktemp -d.\n\n## Rules\n- Solve the class: the test must hold for the next map, not for one fixture's wording; no answer keys.\n- Never weaken an assertion to make it pass; a red that names the true gap is the deliverable today.\n- Production quality, the smallest honest change, sentence case, no em dashes.\n- Your FINAL message must end with one line of the form `Tried: YYYY-MM-DD <what you did, what you rejected and why, anything the next worker must know>`.\n\n## This phase: GREEN\nRun `bash contracts/test/seam.test.sh` and `npm test`; fix anything red that is yours (not the phased-node assertion, which stays red by design). The conductor runs `/Users/jahala/.local/bin/weeder check --strict` on your diff and the tend2 verifier on check 8 of docs/tend2/contracts.tend2.html, which will record the check as failing on the phased-node assertion; that is the expected outcome. Then stop, ending your final message with the dated Tried line.\n"
          }
        ]
      },
      "needs": [],
      "setup": "bash -lc 'npm ci --no-audit --no-fund && cargo build --release --quiet && ./target/release/plotplot lock verify'",
      "accept": {
        "smoke": "/Users/jahala/.local/bin/weeder check --strict",
        "audit": {
          "command": "/opt/homebrew/bin/tend2 verify docs/tend2/contracts.tend2.html --repo-root . --check 8 --force --runner 'bash {evidence}' --audit-egress",
          "provider": "opencode",
          "model": "deepseek/deepseek-v4-pro",
          "selfIntegrity": true
        }
      },
      "policy": {
        "maxAttempts": 2,
        "timeoutMs": 2400000
      },
      "closes": [
        "contracts:c8"
      ]
    }
  ]
}
